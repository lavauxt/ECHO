% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/echo_bam_coverage.R
\name{run_bam_coverage}
\alias{run_bam_coverage}
\title{Extract BAM Coverage}
\usage{
run_bam_coverage(
  bamfiles = NULL,
  bamdir = NULL,
  bed,
  fasta = NULL,
  fasta_source = "file",
  genome_version = NULL,
  bsgenome_cache_dir = NULL,
  rbams = NULL,
  data_out = "./ECHO_coverage.Rdata",
  include.chr = FALSE,
  verbose = TRUE,
  sample_name_delim = "\\\\.",
  sample_name_keep = "1",
  sample_name_collapse = NULL,
  custom_sample_names = NULL,
  bed_zero_based = TRUE,
  skip_invalid_intervals = FALSE,
  pad_terminal_exons = 0
)
}
\arguments{
\item{bamfiles}{Character string. File containing BAM file paths (one per line).}

\item{bamdir}{Character string. Directory containing BAM files.}

\item{bed}{Character string. Path to target BED file.}

\item{fasta}{Character string. Path to reference genome FASTA file.
Required when \code{fasta_source = "file"} (the default); ignored
when \code{fasta_source = "bsgenome"}.}

\item{fasta_source}{One of \code{"file"} (use \code{fasta} directly,
the historical behaviour) or \code{"bsgenome"} (fetch sequence from
an installed Bioconductor BSgenome package for \code{genome_version},
no local FASTA file needed). Default \code{"file"}.}

\item{genome_version}{Character string, e.g. \code{"hg19"} or
\code{"hg38"}. Required when \code{fasta_source = "bsgenome"}.}

\item{bsgenome_cache_dir}{Character string or NULL. Directory used to
cache the BSgenome-derived FASTA across runs. Defaults to a per-user
cache directory. Ignored when \code{fasta_source = "file"}.}

\item{rbams}{Character string. Optional path to external reference BAMs,
either (a) a directory containing \code{.bam} files, or (b) a TSV file
with a \code{bam} column listing reference BAM paths.}

\item{data_out}{Character string. Output \code{.RData} file path.}

\item{include.chr}{Logical. Passed to \code{ExomeDepth::getBamCounts}.}

\item{verbose}{Logical. Print progress messages.}

\item{sample_name_delim}{Character string. Regex delimiter to split filenames.}

\item{sample_name_keep}{Character string. Which parts to keep (e.g., "1", "1-2").}

\item{sample_name_collapse}{Character string. Separator for rejoining parts. Default NULL.}

\item{custom_sample_names}{Optional character vector.}

\item{bed_zero_based}{Logical. If TRUE, BED start coordinates are 0-based.}

\item{skip_invalid_intervals}{Logical. If TRUE, validate each interval against FASTA.}

\item{pad_terminal_exons}{Integer >= 0. Bases to extend the outward-facing
edge of each gene's first and last exon (both edges, for a single-exon
gene), to reduce the chance of a 0/low count right at a gene boundary
where there's no neighbouring exon to carry the signal. Never creates
an overlap with a neighbouring interval or crosses a contig boundary --
padding is applied "if possible" and clamped short otherwise. See
\code{\link{pad_gene_terminal_exons}}. Default \code{0} (disabled).}
}
\value{
Invisibly \code{NULL}. Coverage object saved to disk.
}
\description{
Reads BAM files and computes exon-level read counts using
\code{ExomeDepth::getBamCounts}.
}
